# Curvonomics

A tiny Chrome extension that moves the chat input box to the **top** of the page on **ChatGPT, Claude, and Gemini** — so you type up top and the conversation scrolls below.

Made by [Curv](https://curvlabs.io), the movement company.

## Set it up (30 seconds)

1. **Download ZIP**, then unzip it. *(Or `git clone` the repo, if you must.)*

2. In Chrome, copy **`chrome://extensions`** into the address bar and press Enter. 

3. Turn on **Developer mode** (toggle, top-right).
4. Click **Load unpacked** and select the unzipped folder (the one containing `manifest.json`).
5. Open ChatGPT, Claude, or Gemini — the input is now at the top.

That's it. 

*(If a tab was already open before you installed, refresh it once.)*

## Using it

- It's **on by default**.
- Click the **Curvonomics icon** in the toolbar to toggle it **on / off** anytime.
- New tabs flip automatically — you only ever need to refresh tabs that were already open.

## Works on

| Site | URL |
| --- | --- |
| ChatGPT | `chatgpt.com` |
| Claude | `claude.ai` |
| Gemini | `gemini.google.com` |

## Updating

Pulled new changes? Open `chrome://extensions` and click the **↻ reload** icon on the Curvonomics card.

---

MIT licensed · Not affiliated with OpenAI, Anthropic, or Google.
